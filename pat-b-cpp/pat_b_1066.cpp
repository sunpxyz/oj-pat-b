// pat_b_1066.cpp
// 2017.05.12

/*
#include <iostream>
#include <cstdio>


int main() {
	int m, n, a, b, s;
	scanf("%d%d%d%d%d", &m, &n, &a, &b, &s);

	int dis;
	for (int i = 0; i < m; ++i) {
		for (int j = 0; j < n; ++j) {
			scanf("%d", &dis);
			if (dis >= a && dis <= b) {
				dis = s;
			}

			if (j != n - 1) {
				printf("%03d ", dis);
			} else {
				printf("%03d\n", dis);
			}
		}
	}

	return 0;
}
*/
